fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.28.0
pydantic>=2.7.0
python-dotenv>=1.0.1
python-multipart>=0.0.9

# Database — async SQLAlchemy
# SQLite driver (local dev, zero config)
sqlalchemy[asyncio]==2.0.31
aiosqlite==0.20.0
# Postgres driver (production) — install when DATABASE_URL uses postgresql+asyncpg://
asyncpg==0.29.0

# Task queue — Celery + Redis
# Install when REDIS_URL is configured
celery[redis]==5.4.0
redis==5.0.7

# LLM — OpenAI API (primary)
openai>=1.35.0

# Agent orchestration — LangGraph (Path B)
langgraph>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0

# SSE streaming
sse-starlette>=2.0.0

# MCP (Model Context Protocol) server
mcp>=1.0.0

# Knowledge graph
networkx>=3.3

# Local SLM inference (fallback)
ollama>=0.4.0
